
// This is a basic admin landing page. 
// For this project, the main admin functionality is within the AdminDashboard component on the homepage.
// This page can be expanded for more specific admin sections if needed.

import AdminDashboard from "../../components/dashboards/AdminDashboard";

export default function AdminPage() {
  return <AdminDashboard />;
}
